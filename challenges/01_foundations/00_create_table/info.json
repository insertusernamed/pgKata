{"title": "Creating a table", "difficulty": "easy", "description": "Create the customers table from scratch. Include columns: customer_id (SERIAL PRIMARY KEY), firstname, lastname, username, password, email (all VARCHAR NOT NULL), and created_on (TIMESTAMP DEFAULT CURRENT_TIMESTAMP).", "hints": ["Use CREATE TABLE with column definitions", "Add SERIAL PRIMARY KEY for customer_id", "Include password as a VARCHAR column between username and email"]}
